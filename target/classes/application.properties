# Configuração para MySQL - Local Development
spring.datasource.url=jdbc:mysql://localhost:3306/validacao_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=123456

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

# Configuração de Email para notificações
app.email.admin=theostracke11@gmail.com
app.email.test-mode=true

# Configuração de Email
# Para Gmail, você precisa:
# 1. Ativar autenticação de 2 fatores na sua conta Gmail
# 2. Gerar uma "Senha de app" em: https://myaccount.google.com/apppasswords
# 3. Usar essa senha de app no lugar da sua senha normal

# Configuração de Email para desenvolvimento
# Para produção, use variáveis de ambiente MAIL_USERNAME e MAIL_PASSWORD
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:docu.floww.br@gmail.com}
spring.mail.password=${MAIL_PASSWORD:tvclqonkdhitlwmd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Configuração alternativa para outros provedores:
# Outlook/Hotmail:
# spring.mail.host=smtp-mail.outlook.com
# spring.mail.port=587

# Yahoo:
# spring.mail.host=smtp.mail.yahoo.com
# spring.mail.port=587

# Para desenvolvimento local (MailHog - servidor SMTP fake):
# spring.mail.host=localhost
# spring.mail.port=1025
# spring.mail.username=
# spring.mail.password=

# Upload de arquivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# URLs da aplicação
app.base.url=http://localhost:8080
app.frontend.url=http://localhost:5173

# Upload directory
app.upload.dir=uploads/
