# Banco de dados MySQL no Railway
spring.datasource.url=jdbc:mysql://mysql-c-j4.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=NuryVRUoQwXIItwezIHEqXbUPTxzvgfl
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Caminho para o frontend React buildado (index.html) — só útil se o front for junto no mesmo servidor
spring.web.resources.static-locations=classpath:/static/

# CORS - libera localhost para desenvolvimento e Vercel para produção
app.cors.allowed-origins=http://localhost:5173,https://projeto-tac.vercel.app,https://projeto-tac-production.up.railway.app

# Upload de arquivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/

# Email - produção via variáveis de ambiente MAIL_USERNAME e MAIL_PASSWORD
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Logging
logging.level.com.validacao=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=ERROR

# Porta servidor backend
server.port=8080

# Prefixo global para APIs
server.servlet.context-path=/api
