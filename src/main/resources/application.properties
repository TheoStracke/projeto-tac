# Banco de dados MySQL no Railway (a senha deve ser uma variável de ambiente)
spring.datasource.url=jdbc:mysql://mysql-c-j4.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# CORS
app.cors.allowed-origins=http://localhost:5173,https://projeto-tac.vercel.app,https://projeto-tac-production-8a69.up.railway.app,https://projeto-47gkbaaoh-theostracke11-9910s-projects.vercel.app,https://projeto-tac-ja9q.vercel.app,https://*.vercel.app

# Upload de arquivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/

# Email - produção via variáveis de ambiente
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Logging
logging.level.com.validacao=INFO
logging.level.org.springframework.security=WARN

# Porta do servidor (CRÍTICO PARA A RAILWAY)
server.port=${PORT:8080}

# AWS S3 Storage
aws.s3.bucket=${AWS_S3_BUCKET}
aws.s3.region=${AWS_S3_REGION}
aws.s3.accessKey=${AWS_S3_ACCESS_KEY}
aws.s3.secretKey=${AWS_S3_SECRET_KEY}